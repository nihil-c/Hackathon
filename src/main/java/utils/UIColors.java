package utils;

import java.awt.*;

/**
 * Utility class that defines custom UI colors used throughout the application.
 */
public class UIColors {

    /**
     * Rosso carminio acceso, utilizzato per evidenziare elementi importanti.
     * FUN FACT "CARMINE" Sta per carminio ma anche per Carmine.
     */
    public static final Color CARMINE_RED = new Color(150, 0, 24);

    /**
     * Variante più scura del rosso carminio, tipicamente usata per lo stato "premuto".
     */
    public static final Color PRESSED_CARMINE_RED = new Color(120, 0, 20);

    /**
     * Blu notte, usato come colore principale per sfondi o header.
     */
    public static final Color NIGHT_BLUE = new Color(11, 29, 48);

    /**
     * Grigio chiaro, ideale per sfondi neutri o hover effect.
     */
    public static final Color LIGHT_GRAY = new Color(230, 230, 230);

    public static final Color HYPERLINK_BLUE = new Color(0, 102, 204);
}
